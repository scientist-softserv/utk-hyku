<div class="neutral_repository recent-uploads-div-height recently-uploaded mb-40 p-20">
  <div>
    <h2 class="sr-only"><%= t('hyrax.homepage.recently_uploaded.title') %></h2>
    <% if recent_documents.blank? %>
      <p><%= t('.no_public') %></p>
    <% else %>
      <h3>Recent Uploads</h3>
      <%= render partial: "themes/neutral_repository/hyrax/homepage/recent_document", collection: recent_documents %>
    <% end %>
  </div>
</div>